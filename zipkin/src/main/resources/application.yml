spring:
  application:
    name: zipkin
  profiles:
      active: local

server:
  port: 9411

---
###################  开发环境的profile  ###################
spring:
  profiles: local


logging:
  level:
    root: INFO
    org.springframework.web.servlet.DispatcherServlet: DEBUG
    org.springframework.cloud.sleuth: DEBUG